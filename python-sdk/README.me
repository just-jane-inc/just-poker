# Setup PIP enviornment
`pip install -e .`

# the tui
`poker-tui`

# create a new game
`new-game`

# join a game with test users
`join-game`

# create a game listener
`listen-game`

# start game from id
`start-game`

# get game state
`get-game`

# run tests
`pytest -n auto tests`
`pytest -v -n auto tests`